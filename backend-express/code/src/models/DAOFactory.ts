import { DAOInterface } from '../helpers/dao';

export interface DAOFactoryInterface {
}

const DAOFactory = (
  verbose: boolean,
): DAOFactoryInterface => {
  return {
  };
};

export default DAOFactory;
